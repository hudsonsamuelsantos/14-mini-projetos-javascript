//Faça uma função que retorne o maior entre dois números

let maxValue = returnMax(1,2)
console.log(maxValue)

function returnMax(firstNumber, secondNumber) {
    return firstNumber > secondNumber ? firstNumber : secondNumber
}